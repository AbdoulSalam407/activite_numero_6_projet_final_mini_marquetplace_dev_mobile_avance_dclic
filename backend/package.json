{
  "name": "mini-marketplace-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --port 3001 --routes routes.json",
    "start": "node node_modules/json-server/lib/cli/bin.js --watch db.json --port $PORT --routes routes.json --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
